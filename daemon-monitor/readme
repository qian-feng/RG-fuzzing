To use monitor.py:
$ python monitor.py -i /pwd/to/afl_queue -o output -m /pwd/to/tmp_outdir -- ./Recipe_Database.cov (opt)

# monitor.py use the dynamic build version.
# RG-fuzz use the static build version. Recipe_Database.aif

